#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @m: Parameter value to check if value is absolute
 * Return: Absolute value
 */
int _abs(int m)
{
	if (m < 0)
	{
		return (-m);
	}
	else
	{
		return (m);
	}
}
